# Testing

- Run `{{TEST_CMD}}` before every commit
- Add tests for new behavior; do not delete tests without explicit approval
- Prefer unit tests for business logic; integration tests for API boundaries
- Fix failing tests — do not skip or disable without documenting why
- Keep test files next to source or under a dedicated `tests/` directory per project convention