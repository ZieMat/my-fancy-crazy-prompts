# API Design

- Use consistent naming for routes, handlers, and DTOs
- Validate all external input at the boundary
- Return structured error responses with stable error codes
- Version breaking API changes; document deprecations
- Never expose internal stack traces in production responses