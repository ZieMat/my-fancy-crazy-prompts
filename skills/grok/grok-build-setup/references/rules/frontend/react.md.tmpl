# Frontend (React)

- Prefer functional components with hooks
- Colocate styles with components (CSS modules or project convention)
- Keep components under 200 lines; extract subcomponents when larger
- Avoid prop drilling beyond two levels — use context or composition
- Run `{{LINT_CMD}}` after UI changes